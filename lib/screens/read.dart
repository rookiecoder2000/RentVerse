import 'package:flutter/material.dart';

class ReadAuth extends StatefulWidget {
  ReadAuth({Key? key}) : super(key: key);

  @override
  State<ReadAuth> createState() => _ReadAuthState();
}

class _ReadAuthState extends State<ReadAuth> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ASD"),
      ),
    );
  }
}
